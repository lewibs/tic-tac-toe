function [game] = saveMove(row,col,game)
%saves the last move made
%   row is the row of the last move
%   col is the column of the last move
row=num2str(row);
col=num2str(col);

game = g

% fid = fopen('games.txt','a');
% fprintf(fid,[row,col]);
% fclose(fid);
end

